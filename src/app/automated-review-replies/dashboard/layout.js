export const metadata = {
  title: 'Dashboard | ASOWin Review Replies',
  description: 'Manage your automated review replies with AI',
};

export default function DashboardLayout({ children }) {
  return children;
}
