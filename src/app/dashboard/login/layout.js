export const metadata = {
  title: 'Login | ASOWin Dashboard',
  description: 'Sign in to your ASOWin review reply dashboard',
};

export default function LoginLayout({ children }) {
  return children;
}
